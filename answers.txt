What is the difference between a merge and a rebase?
A merge keeps the change history, while a rebase rewrites the history.

What is a remote repository?
A remote repository is used to store the code and changes between various collaborators.

